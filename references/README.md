# References
Только notes, manifests, hashes, safe metadata/mappings. Не proprietary raw assets.

MM6 slice manifests: `references/mm6/*.manifest.json`.
Raw extract — `references/mm6/raw/` (gitignore);
CLI: `python tools\extract\extract_mm6_text.py`.
